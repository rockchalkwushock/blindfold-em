/**
 * @function Fragment
 * @description wrapper that will yield only children with no added html.
 *
 * @prop {Array} children
 * @returns {Array} children
 */
const Fragment = ({ children }) => children

export default Fragment
